DimeTimetrackerFrontendBundle:
    resource: "@DimeTimetrackerFrontendBundle/Controller/"
    type:     annotation
    prefix:   /

DimeTimetrackerBundle:
    type:     rest
    resource: "@DimeTimetrackerBundle/Resources/config/routing.yml"
    prefix:   /api
    defaults: { _format: json }

DimeTimetrackerInvoiceBundle:
    resource: "@DimeTimetrackerInvoiceBundle/Resources/config/routing.yml"
    prefix:   /

# Internal routing configuration to handle ESI
#_internal:
#   resource: "@FrameworkBundle/Resources/config/routing/internal.xml"
#   prefix:   /_internal
