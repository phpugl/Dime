Router system
=============

The application router is a extented Backbone.Router and accessable via *Dime.router*.

.. code-block:: js
    :linenos:

    Dime.router.route('#route', 'name', callback);
