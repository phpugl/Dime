{% extends "DimeTimetrackerFrontendBundle::layout.html.twig" %}

{% block content %}
<h1>Service</h1>
<table class="zebra-striped" id="dataTable">
    <thead>
        <tr>
            <th>#</th>
            <th>Name</th>
            <th>Rate</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for service in services %}
    <tr>
        <td>{{ service.id }}</td>
        <td>{{ service.name }}</td>
        <td>{{ service.rate }}</td>
        <td>
        <a href="/api/services/{{ service.id }}.json" class="data-edit" data-controls-modal="edit-modal" data-backdrop="static">Edit</a>
         | <a href="/api/services/{{ service.id }}.json" class="data-delete">Delete</a>
        </td>
    </tr>
    {% endfor %}
    </tbody>
</table>

{% endblock %}

{% block sidebar %}
<h2><a href="{{ path('post_services') }}">Add Service</a></h2>

<h2>Filter</h2>
TODO
{% endblock %}
